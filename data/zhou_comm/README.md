# 周炯槃《通信原理》原子库（只读交付）

验收用交付物，来自 `/workspace/zhou-comm-atoms/`。

- **不**写入 `teaching.db`
- **不**接线（无 `zhou_sqlite` backend）
- **不**上教学云

详见 `HANDOFF.md`。冒烟：

```bash
cd data/zhou_comm
python3 query_api.py demo
python3 query_api.py bench
```
